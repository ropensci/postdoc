1.4.1
  - Give a warning instead of an error for unsupported image types

1.4.0
  - Use CDN for katex.min.css because it also needs font files
  - Use latest katex

1.3.0
  - Remove <main> tags from each page

1.2.2
  - Use https://api.cran.dev for faster link lookups

1.2.0
  - Small fixes for weird edge cases

1.1.0
  - Do not include 'internal' pages in the manual
  - Parse orcid links in author fields
  - Warn (instead of error) for broken topic links
  - Move help pages with name '*-package' to the top

1.0.0
  - Initial release
